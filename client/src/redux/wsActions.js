
// export const wsConnect = host => ({ type: 'WS_CONNECT', host });
// export const wsConnecting = host => ({ type: 'WS_CONNECTING', host });
// export const wsConnected = host => ({ type: 'WS_CONNECTED', host });
// export const wsDisconnect = host => ({ type: 'WS_DISCONNECT', host });
// export const wsDisconnected = host => ({ type: 'WS_DISCONNECTED', host });

// export const wsSubscribeRoomsList = host => ({ type: 'WS_SUBSCRIBE_ROOMS_LIST', host });
// export const wsUnsubscribeRoomsList = host => ({ type: 'WS_UNSUBSCRIBE_ROOMS_LIST', host });
// export const wsCreateRoom = state => ({ type: 'WS_CREATE_ROOM', state });

// export const wsSubscribeRoom = roomId => ({ type: 'WS_SUBSCRIBE_ROOM', roomId });
// export const wsSubscribedRoom = () => ({type: 'WS_ROOM_SUBSCRIBED'});
// export const wsUnsubscribeRoom = host => ({ type: 'WS_UNSUBSCRIBE_ROOM', host });
// export const wsUnsubscribedRoom = () => ({type: 'WS_ROOM_UNSUBSCRIBED'});

// export const wsSend = msg => ({ type: 'NEW_MESSAGE', msg });