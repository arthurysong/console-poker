TODO:
Should I not send cards to JSON? and create separate subscription for a user's cards?
Eh I don't think it matters too much as long as it's not being displayed in console.

? User validations
? REGEX expression for handleAuthRedirect for /rooms url
? Sign in via Google drive?

CREATE A MACRO FETCH WITH TOKEN FUNCTION